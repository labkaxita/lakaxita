[buildout]
extends = buildout.cfg

parts +=
    rubygems

eggs +=
    django_nose
    django-debug-toolbar
    selenium

[django]
wsgi = false
settings = settings.development

[rubygems]
recipe = rubygemsrecipe
gems = sass
