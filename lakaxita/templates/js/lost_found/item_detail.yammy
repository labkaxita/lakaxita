{% load i18n %}

{% verbatim %}
    section.left
        img.slide
            - src {{ image }}
        {{#if status}}
    section.right
        h3 
            | {{ title }}
            img.status
                - src {{ status }}
            {{/if}}
        p.hover 
            | Date of discovery: {{ hover }}
        div.body
            {{{ description }}}

{% endverbatim %}
button.form 
    -value it's mine!
    | It's mine!
