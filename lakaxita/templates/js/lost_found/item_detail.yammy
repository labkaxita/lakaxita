{% load i18n %}

{% verbatim %}
    section.left
        img.slide
            - src {{ image }}
        div#return
            img.status
                - src {{ status }}
            {{#if status}}

            {% endverbatim %}
            | {% trans 'It has been already returned' %}
            {% verbatim %}

            {{else}}

            {% endverbatim %}
            | {% trans 'It has not been returned yet' %}
            {% verbatim %}

            {{/if}}

    section.right
        h3 
            | {{ title }}
        p.hover 
            | Date of discovery: {{ hover }}

        div.body
            {{{ description }}}

{% endverbatim %}
button.form 
    | {% trans 'It is mine!' %}
